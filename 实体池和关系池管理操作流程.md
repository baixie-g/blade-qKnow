# 实体池和关系池管理操作流程

## 📋 概述

本文档描述了gKnow系统中实体池和关系池管理的完整操作流程，包括从抽取到审核再到Neo4j存储的全过程。

## 🎯 功能特性

### ✅ 实体池管理功能
- **实体抽取**：从文档中自动抽取实体信息
- **实体审核**：支持单个和批量确认/拒绝实体
- **实体消歧**：调用外部API进行实体消歧，获取候选实体
- **Neo4j集成**：确认的实体自动存入Neo4j数据库
- **状态管理**：待处理(0)、已确认(1)、已拒绝(2)

### ✅ 关系池管理功能
- **关系抽取**：从文档中自动抽取实体间关系
- **关系审核**：支持单个和批量确认/拒绝关系
- **依赖检查**：关系确认前检查源实体和目标实体是否已确认
- **Neo4j集成**：确认的关系自动存入Neo4j数据库
- **状态管理**：待处理(0)、已确认(1)、已拒绝(2)

## 🔄 完整操作流程

### 1. 非结构化抽取任务执行

```bash
# 1. 创建非结构化抽取任务
POST /ext/unstructTask/create
{
  "name": "测试任务",
  "workspaceId": 1,
  "docIds": "1,2,3",
  "relationIds": "1,2,3"
}

# 2. 执行抽取任务
POST /ext/unstructTask/executeExtraction
{
  "id": 3
}

# 3. 手动触发队列消费（可选）
POST /ext/unstructTask/manualConsumeQueue
```

### 2. 实体池管理操作

#### 2.1 查看实体池列表
```bash
GET /ext/entityPool/page?taskId=3&status=0&pageNo=1&pageSize=10
```

#### 2.2 单个实体处理
```bash
# 确认实体
POST /ext/entityPool/process?id=1&status=1&remark=确认该实体

# 拒绝实体
POST /ext/entityPool/process?id=2&status=2&remark=实体信息不准确
```

#### 2.3 批量实体处理
```bash
POST /ext/entityPool/batch-process
{
  "idList": [1, 2, 3, 4, 5],
  "status": 1,
  "remark": "批量确认实体"
}
```

#### 2.4 实体消歧操作
```bash
# 获取候选实体
POST /ext/entityPool/disambiguate?entityPoolId=1&topK=5

# 确认消歧结果
POST /ext/entityPool/confirm-disambiguation?entityPoolId=1&candidateId=entity_001&remark=选择最匹配的实体
```

### 3. 关系池管理操作

#### 3.1 查看关系池列表
```bash
GET /ext/relationshipPool/page?taskId=3&status=0&pageNo=1&pageSize=10
```

#### 3.2 单个关系处理
```bash
# 确认关系
POST /ext/relationshipPool/process?id=1&status=1&remark=确认该关系

# 拒绝关系
POST /ext/relationshipPool/process?id=2&status=2&remark=关系类型错误
```

#### 3.3 批量关系处理
```bash
POST /ext/relationshipPool/batch-process
{
  "idList": [1, 2, 3, 4, 5],
  "status": 1,
  "remark": "批量确认关系"
}
```

#### 3.4 查看实体相关关系
```bash
GET /ext/relationshipPool/by-entity?entityId=person_001&taskId=3
```

## 🗄️ Neo4j数据库维护

### 实体节点结构
```cypher
// 实体节点标签和属性
CREATE (n:DynamicEntity:ExtUnStruck {
  name: "小明",
  entity_id: "person_001",
  entity_type: "人物",
  task_id: 3,
  doc_id: 1,
  paragraph_index: 1,
  workspace_id: 1,
  release_status: 1,
  process_time: "2025-01-20T10:00:00",
  process_by: "admin",
  aliases: "[]",
  definition: "人物实体",
  attributes: "{}"
})
```

### 关系结构
```cypher
// 关系结构
CREATE (source:DynamicEntity:ExtUnStruck {entity_id: "person_001"})
CREATE (target:DynamicEntity:ExtUnStruck {entity_id: "organization_001"})
CREATE (source)-[r:毕业院校 {
  relationship_type: "毕业院校",
  task_id: 3,
  doc_id: 1,
  paragraph_index: 1,
  workspace_id: 1,
  release_status: 1,
  process_time: "2025-01-20T10:00:00",
  process_by: "admin"
}]->(target)
```

## 🔧 配置说明

### 实体消歧API配置
```yaml
disambiguation:
  api:
    url: http://localhost:8002/match-candidates
    timeout: 10000
    retry-count: 3
```

### 抽取API配置
```yaml
extraction:
  api:
    url: http://127.0.0.1:8000/extract
    timeout: 30000
    retry-count: 3
```

## 📊 数据状态说明

### 实体状态
- `0`: 待处理 - 刚抽取的实体，等待人工审核
- `1`: 已确认 - 人工确认的实体，已存入Neo4j
- `2`: 已拒绝 - 人工拒绝的实体，不会存入Neo4j

### 关系状态
- `0`: 待处理 - 刚抽取的关系，等待人工审核
- `1`: 已确认 - 人工确认的关系，已存入Neo4j（前提是源实体和目标实体都已确认）
- `2`: 已拒绝 - 人工拒绝的关系，不会存入Neo4j

## 🚀 最佳实践

### 1. 操作顺序建议
1. 先处理实体池中的实体，确认重要的实体
2. 再处理关系池中的关系，系统会自动检查实体状态
3. 使用批量操作提高效率
4. 对于有歧义的实体，使用消歧功能

### 2. 数据质量保证
- 定期检查Neo4j中的数据完整性
- 使用关系查询验证实体间的连接
- 监控抽取质量，及时调整抽取配置

### 3. 性能优化
- 合理设置批量处理的数量
- 定期清理已拒绝的数据
- 监控Neo4j数据库性能

## 🔍 常见问题

### Q1: 关系确认失败怎么办？
A: 检查源实体和目标实体是否都已确认。只有两个实体都确认后，关系才能存入Neo4j。

### Q2: 实体消歧API调用失败怎么办？
A: 检查消歧API服务是否正常运行，网络连接是否正常，API配置是否正确。

### Q3: Neo4j数据不一致怎么办？
A: 检查实体和关系的处理状态，确保数据同步。可以重新处理相关实体和关系。

### Q4: 如何批量导入数据？
A: 使用导入功能，支持Excel格式的数据导入，可以选择是否更新已存在的数据。

## 📈 监控和日志

### 关键日志位置
- 实体处理日志：`qknow-server/logs/info.log`
- 关系处理日志：`qknow-server/logs/info.log`
- Neo4j操作日志：`qknow-server/logs/info.log`

### 监控指标
- 实体抽取数量
- 关系抽取数量
- 实体确认率
- 关系确认率
- Neo4j存储成功率

## 🎉 总结

通过以上完整的操作流程，用户可以：
1. 高效地管理从文档抽取的实体和关系
2. 确保数据质量，通过人工审核过滤错误数据
3. 自动维护Neo4j数据库，保证数据一致性
4. 支持批量操作，提高工作效率
5. 提供完整的API接口，便于系统集成

这套完整的实体池和关系池管理系统为gKnow提供了强大的知识图谱构建能力。 