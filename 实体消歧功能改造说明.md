# 实体消歧功能改造说明

## 概述

本次改造为gKnow项目添加了完整的实体消歧功能，实现了从文档抽取到人工审核再到存入Neo4j的完整流程。

## 主要改造内容

### 1. 后端改造

#### 1.1 实体池服务增强 (`ExtEntityPoolServiceImpl.java`)

**新增功能：**
- ✅ 实体消歧API调用 (`disambiguateEntity`)
- ✅ 消歧结果确认 (`confirmDisambiguation`)
- ✅ 实体确认后自动存入Neo4j (`saveEntityToNeo4j`)
- ✅ 相关关系自动处理 (`processRelatedRelationships`)
- ✅ 事务保证数据一致性

**关键方法：**
```java
// 实体消歧
public AjaxResult disambiguateEntity(Long entityPoolId, Integer topK)

// 确认消歧结果
public AjaxResult confirmDisambiguation(Long entityPoolId, String candidateId, String remark)

// 处理实体（确认/拒绝）
public AjaxResult processEntity(Long id, Integer status, String remark)
```

#### 1.2 数据库层面增强

**实体池Mapper (`ExtEntityPoolMapper.java`):**
- ✅ 添加按实体ID查询方法：`selectByEntityId`

**关系池服务 (`ExtRelationshipPoolServiceImpl.java`):**
- ✅ 添加按实体ID查询关系方法：`getRelationshipsByEntityId`

#### 1.3 控制器层增强 (`ExtEntityPoolController.java`)

**新增API接口：**
```java
// 实体消歧 - 获取候选实体
@PostMapping("/disambiguate")
public AjaxResult disambiguateEntity(@RequestParam("entityPoolId") Long entityPoolId,
                                    @RequestParam(value = "topK", defaultValue = "5") Integer topK)

// 实体消歧 - 确认消歧结果
@PostMapping("/confirm-disambiguation")
public AjaxResult confirmDisambiguation(@RequestParam("entityPoolId") Long entityPoolId,
                                       @RequestParam("candidateId") String candidateId,
                                       @RequestParam(value = "remark", required = false) String remark)
```

#### 1.4 配置文件更新

**开发环境 (`application-ext-dev.yml`):**
```yaml
# 实体消歧API配置
disambiguation:
  api:
    url: http://localhost:8002/match-candidates
```

**生产环境 (`application-ext-prod.yml`):**
```yaml
# 实体消歧API配置
disambiguation:
  api:
    url: http://localhost:8002/match-candidates
```

### 2. 前端改造

#### 2.1 API接口文件 (`qknow-ui/src/api/ext/extEntityPool/index.js`)

**新增接口：**
```javascript
// 实体消歧 - 获取候选实体
export function disambiguateEntity(entityPoolId, topK = 5)

// 实体消歧 - 确认消歧结果
export function confirmDisambiguation(entityPoolId, candidateId, remark)
```

#### 2.2 页面功能增强 (`qknow-ui/src/views/ext/extEntityPool/index.vue`)

**新增功能：**
- ✅ 消歧按钮（仅对待处理实体显示）
- ✅ 实体消歧对话框（显示候选实体列表）
- ✅ 候选实体选择功能
- ✅ 消歧结果确认对话框
- ✅ 相似度可视化显示

**新增对话框：**
1. **实体消歧对话框**：显示待消歧实体信息和候选实体列表
2. **确认消歧结果对话框**：确认选择的候选实体

## 数据流程

### 1. 文档抽取流程
```
文档上传 → 三元组抽取 → 存入实体池/关系池 → 待人工审核
```

### 2. 实体消歧流程
```
选择待处理实体 → 点击消歧按钮 → 调用消歧API → 显示候选实体 → 选择候选实体 → 确认消歧结果 → 更新实体信息 → 存入Neo4j → 处理相关关系
```

### 3. 实体确认流程
```
选择待处理实体 → 点击确认按钮 → 直接确认 → 存入Neo4j → 处理相关关系
```

## 消歧API格式

### 请求格式
```json
{
  "entity": {
    "aliases": [],
    "attributes": {
      "并发症": ["记忆丢失"],
      "治疗方法": ["他人照顾"],
      "症状": ["情绪不平稳"]
    },
    "definition": "老年人经常得到",
    "name": "老呆呆",
    "source": "民间口述",
    "type": "疾病"
  },
  "include_scores": true,
  "top_k": 5
}
```

### 响应格式
```json
{
  "candidates": [
    {
      "entity": {
        "aliases": ["diabetes", "糖尿"],
        "attributes": {
          "并发症": ["视网膜病变", "肾病"],
          "治疗方法": ["饮食控制", "胰岛素治疗"],
          "症状": ["多尿", "口渴", "体重下降"]
        },
        "create_time": "2024-06-10T10:00:00",
        "definition": "糖尿病是一组以高血糖为特征的代谢性疾病...",
        "id": "disease_00123",
        "name": "糖尿病",
        "source": "临床指南-2022",
        "type": "疾病"
      },
      "rank": 1,
      "score": {
        "bge_score": 0.92,
        "cross_encoder_score": 0.88,
        "final_score": 0.91,
        "fuzz_score": 0.95,
        "levenshtein_score": 0.9
      },
      "similarity_details": "名称完全匹配，语义相似度很高"
    }
  ],
  "message": "匹配候选实体完成",
  "total_count": 1
}
```

## 权限配置

需要在权限管理中配置以下权限：
- `ext:entityPool:disambiguate` - 实体消歧权限
- `ext:entityPool:confirm` - 确认消歧结果权限
- `ext:entityPool:process` - 处理实体权限

## 部署说明

### 1. 后端部署
1. 确保实体消歧服务已启动并监听在 `http://localhost:8002`
2. 更新配置文件中的消歧API地址
3. 重启后端服务

### 2. 前端部署
1. 重新构建前端项目
2. 部署到Web服务器

## 测试验证

### 1. 功能测试
1. 上传文档进行抽取
2. 在实体池页面查看待处理实体
3. 点击消歧按钮测试消歧功能
4. 选择候选实体并确认
5. 验证实体已存入Neo4j

### 2. 异常测试
1. 消歧服务不可用时的错误处理
2. 网络异常时的重试机制
3. 数据一致性验证

## 注意事项

1. **数据一致性**：使用事务保证实体和关系的处理一致性
2. **错误处理**：完善的异常处理和用户友好的错误提示
3. **性能优化**：消歧API调用使用异步处理，避免阻塞用户界面
4. **安全性**：API调用添加超时和重试机制

## 后续优化建议

1. **批量处理**：支持批量实体消歧
2. **历史记录**：记录消歧历史，支持撤销操作
3. **智能推荐**：基于历史数据优化消歧算法
4. **性能监控**：添加消歧性能监控和统计
5. **缓存机制**：对常用实体进行缓存，提高响应速度 